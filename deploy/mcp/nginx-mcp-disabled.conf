# Fail closed while the native MCP candidate is absent, unhealthy, or rolling back.
location = /mcp { access_log off; return 404; }
location = /.well-known/oauth-protected-resource { access_log off; return 404; }
location = /.well-known/oauth-protected-resource/mcp { access_log off; return 404; }
location = /.well-known/oauth-authorization-server { access_log off; return 404; }
location = /oauth/authorize { access_log off; return 404; }
location = /oauth/token { access_log off; return 404; }
location = /oauth/revoke { access_log off; return 404; }
location = /oauth/register { access_log off; return 404; }
location ^~ /oauth/ { access_log off; return 404; }
